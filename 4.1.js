function calcSurface(a, b){
    return a * b;
}

let a = window.prompt ("Give me the length");
let b = window.prompt ("Give me the height");

console.log("The surface of the rectangle is " + calcSurface(a, b))


